# Netlify config for the static demo.
# The app uses hash routing in demo mode, so no SPA redirect is required,
# but this keeps direct hits on any path working too.
[build]
  command = "npm run build:demo"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
