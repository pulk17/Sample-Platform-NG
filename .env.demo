# Loaded by `vite build --mode demo` (npm run build:demo). Turns on the
# baked-snapshot fetch layer so the app runs with no backend. Works the same
# on Windows and Unix — no shell-specific env prefix needed.
VITE_DEMO=1
